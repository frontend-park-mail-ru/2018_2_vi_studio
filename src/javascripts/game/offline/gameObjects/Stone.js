export default class Stone {
    constructor(type, tile, row, col, gate) {
        this.type = type;
        this.tile = tile;
        this.row = row;
        this.col = col;
        this.gate = gate;
    }
}